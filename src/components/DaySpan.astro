---
import Day from "./Day.astro";

export interface Props {
  from: Date;
  to: Date;
}

const { from, to } = Astro.props;
---

{
  from === to ? (
    <Day date={from} />
  ) : (
    <>
      <Day date={from} /> 〜<Day date={to} />
    </>
  )
}
